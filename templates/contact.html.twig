{% extends "base.html.twig" %}

{% block body %}

<div class="row justify-content-center">
  <div class="col-lg-6 col-md-8 col-sm-10 col-12 pt-4 pb-3 text-light">
    <h1 class="display-6 mb-4 text-muted">Contact Me</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group row">
      <div class="col-sm-2">
      </div>
      <div class="col-sm-10">
        <input type="submit" value="Send" class="btn btn-info">
      </div>
    {{ form_end(form) }}
</div>

{% endblock %}
